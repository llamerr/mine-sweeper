import styled from '@emotion/styled';
import { Button } from '@mui/material';
import SendIcon from '@mui/icons-material/Send';

export const SubmitButton = styled(Button)`
  padding: 0.5rem;
  margin: 0.5rem;
`

export const SubmitButtonIcon = styled(SendIcon)`
  margin-left: 0.5rem;
`
